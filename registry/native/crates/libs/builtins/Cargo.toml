[package]
name = "secureexec-builtins"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Built-in command implementations (sleep, test/[, whoami) for secure-exec WasmVM"

[dependencies]
wasi-ext = { path = "../../wasi-ext" }
