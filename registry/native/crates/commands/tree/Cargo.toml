[package]
name = "cmd-tree"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "tree standalone binary for secure-exec WasmVM"

[[bin]]
name = "tree"
path = "src/main.rs"

[dependencies]
secureexec-tree = { path = "../../libs/tree" }
