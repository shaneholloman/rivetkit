[package]
name = "cmd-diff"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "diff standalone binary for secure-exec WasmVM"

[[bin]]
name = "diff"
path = "src/main.rs"

[dependencies]
secureexec-diff = { path = "../../libs/diff" }
