[package]
name = "cmd-whoami"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "whoami standalone binary for secure-exec WasmVM"

[[bin]]
name = "whoami"
path = "src/main.rs"

[dependencies]
secureexec-builtins = { path = "../../libs/builtins" }
