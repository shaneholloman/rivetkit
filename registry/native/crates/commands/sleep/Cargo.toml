[package]
name = "cmd-sleep"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "sleep standalone binary for secure-exec WasmVM"

[[bin]]
name = "sleep"
path = "src/main.rs"

[dependencies]
secureexec-builtins = { path = "../../libs/builtins" }
