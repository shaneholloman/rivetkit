[package]
name = "cmd-tar"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "tar standalone binary for secure-exec WasmVM"

[[bin]]
name = "tar"
path = "src/main.rs"

[dependencies]
secureexec-tar = { path = "../../libs/tar" }
